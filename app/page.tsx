export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 24 }}>
      <h1>Browser Worker</h1>
      <p>Use <code>/api/health</code> and <code>/api/run</code>.</p>
    </main>
  );
}
